[uwsgi]
enable-threads = true
module = checker
callable = app
uid = root
gid = root

threads = 20
cheaper-algo = spare
cheaper = 50
cheaper-initial = 50
# maximum number of workers that can be spawned
workers = 100
# how many workers should be spawned at a time
cheaper-step = 20

lazy-apps = true